extends layout

block content
  h1 #{title}
  form(method='POST', action='/articles/edit_comment/'+comment._id)
    #form-group
      label Body:
      textarea.form-control(name='comment')= comment.comment
    br
    input.btn.btn-primary(type='submit',value='Submit')
